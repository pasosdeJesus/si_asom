<%= f.input :bushabilitado,
  collection: ['SI', 'NO', 'Todos'],
  label: false,
  selected: Msip::ModeloHelper.poromision(params, :bushabilitado),
  input_html: {
    class: 'tom-select',
    'data-enviarautomatico' => '',
    value: params && params[:filtro] && params[:filtro][:habilitado] ? 
      params[:filtro][:habilitado] : 'SI'
  },
  include_blank: false
%>
